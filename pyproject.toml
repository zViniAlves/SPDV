[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "SPDV"
version = "0.1.0"
description = "Sistema de previsao de vendas"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.31",
]

[tool.setuptools.packages.find]
where = ["src"]